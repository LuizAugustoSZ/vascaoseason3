(function(){
const editor=document.getElementById('news-editor'),form=document.getElementById('news-form');if(!editor||!form)return;
const coverFile=document.getElementById('cover-file'),coverData=document.getElementById('cover-data'),coverPreview=document.getElementById('cover-preview'),bodyImage=document.getElementById('body-image');let savedRange=null;
function compressNewsImage(file,maxWidth,maxHeight,quality=.78){return new Promise((resolve,reject)=>{const image=new Image(),url=URL.createObjectURL(file);image.onload=()=>{let width=image.width,height=image.height;const scale=Math.min(1,maxWidth/width,maxHeight/height);width=Math.round(width*scale);height=Math.round(height*scale);const canvas=document.createElement('canvas');canvas.width=width;canvas.height=height;canvas.getContext('2d').drawImage(image,0,0,width,height);URL.revokeObjectURL(url);resolve(canvas.toDataURL('image/webp',quality));};image.onerror=()=>{URL.revokeObjectURL(url);reject(new Error('Não foi possível abrir a imagem.'));};image.src=url;});}
document.querySelectorAll('#tab-noticias .editor-toolbar [data-command]').forEach(button=>button.addEventListener('click',()=>{editor.focus();document.execCommand(button.dataset.command,false);}));
document.querySelectorAll('#tab-noticias .editor-toolbar [data-block]').forEach(button=>button.addEventListener('click',()=>{editor.focus();document.execCommand('formatBlock',false,button.dataset.block);}));
document.getElementById('insert-image')?.addEventListener('click',()=>{const selection=window.getSelection();if(selection.rangeCount)savedRange=selection.getRangeAt(0).cloneRange();bodyImage.click();});
coverFile.addEventListener('change',async()=>{if(!coverFile.files[0])return;try{coverData.value=await compressNewsImage(coverFile.files[0],1200,700,.72);coverPreview.src=coverData.value;coverPreview.classList.remove('d-none');}catch(error){showNewsToast(error.message,'danger');}});
bodyImage.addEventListener('change',async()=>{if(!bodyImage.files[0])return;try{const source=await compressNewsImage(bodyImage.files[0],1200,1000,.72);editor.focus();const selection=window.getSelection();if(savedRange){selection.removeAllRanges();selection.addRange(savedRange);}document.execCommand('insertHTML',false,`<p><img src="${source}" alt="Imagem da notícia"></p><p><br></p>`);bodyImage.value='';}catch(error){showNewsToast(error.message,'danger');}});
const resetNewsForm=()=>{form.reset();form.noticia_id.value='';coverData.value='';coverPreview.src='';coverPreview.classList.add('d-none');editor.innerHTML='<p>Escreva a notícia aqui...</p>';document.getElementById('news-submit').textContent='Publicar notícia';document.getElementById('news-editing').classList.add('d-none');document.getElementById('news-editing').classList.remove('d-flex');};
document.getElementById('cancel-news-edit')?.addEventListener('click',resetNewsForm);
let news=[];try{news=JSON.parse(document.getElementById('news-admin-data')?.textContent||'[]');}catch(error){news=[];}
document.querySelectorAll('.editar-noticia').forEach(button=>button.addEventListener('click',()=>{const item=news.find(entry=>String(entry.id)===button.dataset.id);if(!item)return;form.noticia_id.value=item.id;form.titulo.value=item.titulo;form.resumo.value=item.resumo;coverData.value=item.capa_base64;coverPreview.src=item.capa_base64;coverPreview.classList.remove('d-none');editor.innerHTML=item.conteudo;document.getElementById('news-submit').textContent='Atualizar notícia';const notice=document.getElementById('news-editing');notice.querySelector('span').textContent=`Editando: ${item.titulo}`;notice.classList.remove('d-none');notice.classList.add('d-flex');form.scrollIntoView({behavior:'smooth',block:'start'});}));
form.addEventListener('submit',async event=>{event.preventDefault();event.stopImmediatePropagation();if(!coverData.value){showNewsToast('Selecione uma imagem de capa.','danger');return;}document.getElementById('news-content').value=editor.innerHTML;const button=event.submitter||document.getElementById('news-submit'),old=button.textContent;button.disabled=true;button.textContent='Salvando...';try{const payload=new FormData(form);payload.set('_ajax','1');const response=await fetch('index.php',{method:'POST',body:payload,headers:{Accept:'application/json'},credentials:'same-origin'}),data=await response.json();if(!response.ok||!data.ok)throw new Error(data.message||'Não foi possível salvar.');await refreshNewsTab(data.message);}catch(error){button.disabled=false;button.textContent=old;showNewsToast(error.message,'danger');}});
document.querySelectorAll('.admin-news-list form[method="post"]').forEach(deleteForm=>deleteForm.addEventListener('submit',async event=>{event.preventDefault();event.stopImmediatePropagation();if(!confirm('Remover esta notícia do jornal?'))return;try{const payload=new FormData(deleteForm);payload.set('_ajax','1');const response=await fetch('index.php',{method:'POST',body:payload,headers:{Accept:'application/json'},credentials:'same-origin'}),data=await response.json();if(!response.ok||!data.ok)throw new Error(data.message||'Não foi possível apagar.');await refreshNewsTab(data.message);}catch(error){showNewsToast(error.message,'danger');}}));
const list=document.querySelector('.admin-news-list');if(list){const items=[...list.querySelectorAll(':scope > article')];if(items.length){const controls=document.createElement('div');controls.className='p-3 border-bottom';controls.innerHTML='<input class="form-control form-control-sm" type="search" placeholder="Pesquisar notícia ou autor...">';list.before(controls);const pager=document.createElement('div');pager.className='d-flex justify-content-between align-items-center p-3 border-top';list.after(pager);let page=1;const render=()=>{const q=controls.querySelector('input').value.toLocaleLowerCase('pt-BR').trim(),filtered=items.filter(item=>!q||item.textContent.toLocaleLowerCase('pt-BR').includes(q)),pages=Math.max(1,Math.ceil(filtered.length/5));page=Math.min(page,pages);const visible=new Set(filtered.slice((page-1)*5,page*5));items.forEach(item=>item.classList.toggle('d-none',!visible.has(item)));pager.innerHTML=`<span class="text-secondary small">${filtered.length} postagem${filtered.length===1?'':'s'}</span>${filtered.length>5?`<div class="d-flex gap-2 align-items-center"><button class="btn btn-sm btn-outline-light prev" ${page===1?'disabled':''}>Anterior</button><span class="small text-secondary">Página ${page} de ${pages}</span><button class="btn btn-sm btn-outline-light next" ${page===pages?'disabled':''}>Próxima</button></div>`:''}`;};controls.addEventListener('input',()=>{page=1;render();});pager.addEventListener('click',event=>{if(event.target.closest('.prev'))page--;else if(event.target.closest('.next'))page++;else return;render();});render();}}
async function refreshNewsTab(message){const response=await fetch(`index.php?tab=noticias&_refresh=${Date.now()}`,{cache:'no-store',credentials:'same-origin'}),html=await response.text(),parsed=new DOMParser().parseFromString(html,'text/html'),fresh=parsed.getElementById('tab-noticias');if(!response.ok||!fresh)throw new Error('A notícia foi salva, mas não foi possível atualizar a listagem.');fresh.classList.add('show','active');document.getElementById('tab-noticias').replaceWith(fresh);history.replaceState(null,'','index.php?tab=noticias');const script=document.createElement('script');script.src=`../assets/js/news-editor.js?v=${Date.now()}`;document.body.append(script);showNewsToast(message,'success');}
function showNewsToast(message,type){const toast=document.createElement('div');toast.className=`alert alert-${type} position-fixed top-0 start-50 translate-middle-x mt-3 shadow`;toast.style.zIndex='2000';toast.textContent=message;document.body.append(toast);setTimeout(()=>toast.remove(),4000);}
})();
