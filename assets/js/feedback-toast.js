(()=>{const duration=5000;let stack;const getStack=()=>{if(stack?.isConnected)return stack;stack=document.createElement('div');stack.className='feedback-toast-stack';stack.setAttribute('aria-live','polite');stack.setAttribute('aria-atomic','false');document.body.append(stack);return stack};const normalizeType=type=>['success','danger','warning','info'].includes(type)?type:'info';window.siteToast=(message,type='info')=>{if(!message)return null;const toast=document.createElement('div');toast.className=`alert alert-${normalizeType(type)} feedback-toast`;toast.setAttribute('role',type==='danger'?'alert':'status');toast.innerHTML='<div class="feedback-toast__message"></div><button class="feedback-toast__close" type="button" aria-label="Fechar aviso">&times;</button><span class="feedback-toast__timer" aria-hidden="true"></span>';const content=toast.querySelector('.feedback-toast__message');if(message instanceof Node)content.append(message);else content.textContent=String(message);let timer;const dismiss=()=>{if(toast.classList.contains('is-leaving'))return;clearTimeout(timer);toast.classList.add('is-leaving');setTimeout(()=>toast.remove(),210)};toast.querySelector('.feedback-toast__close').addEventListener('click',dismiss);toast.addEventListener('mouseenter',()=>clearTimeout(timer));toast.addEventListener('mouseleave',()=>{timer=setTimeout(dismiss,duration)});getStack().append(toast);timer=setTimeout(dismiss,duration);return toast};const promote=element=>{if(!(element instanceof Element)||!element.matches('[data-flash-toast]')||element.dataset.flashPromoted==='1')return;element.dataset.flashPromoted='1';const type=['success','danger','warning','info'].find(item=>element.classList.contains(`alert-${item}`))||'info';const content=document.createElement('div');content.innerHTML=element.innerHTML;element.remove();window.siteToast(content,type)};const scan=root=>{if(root instanceof Element)promote(root);root.querySelectorAll?.('[data-flash-toast]').forEach(promote)};const start=()=>{scan(document);new MutationObserver(records=>records.forEach(record=>record.addedNodes.forEach(node=>{if(node instanceof Element)scan(node)}))).observe(document.body,{childList:true,subtree:true})};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start()})();
