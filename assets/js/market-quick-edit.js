document.addEventListener('DOMContentLoaded',()=>{
  const cards=[...document.querySelectorAll('.competition-roster .registration-card[data-player-id]')].filter(card=>Number(card.dataset.playerId)>0);if(!cards.length)return;
  const csrf=document.querySelector('.competition-roster input[name="csrf"]')?.value||'',championship=document.querySelector('.competition-roster input[name="campeonato_id"]')?.value||'',participant=document.querySelector('.competition-roster input[name="participante_id"]')?.value||'';
  const modal=document.createElement('div');modal.className='modal fade general-dark-modal market-player-edit-modal';modal.id='market-player-edit-modal';modal.tabIndex=-1;modal.innerHTML=`<div class="modal-dialog modal-lg modal-dialog-centered"><div class="modal-content"><form method="post"><div class="modal-header"><div><small class="eyebrow">Dados oficiais da carta</small><h2 class="modal-title">EDITAR JOGADOR</h2></div><button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Fechar"></button></div><div class="modal-body"><input type="hidden" name="csrf" value="${csrf}"><input type="hidden" name="action" value="editar_jogador_rapido"><input type="hidden" name="campeonato_id" value="${championship}"><input type="hidden" name="jogador_geral_id">${participant?`<input type="hidden" name="participante_id" value="${participant}">`:''}<div class="alert alert-warning"><strong>AVISO: COPIE EXATAMENTE O NOME DA CARTA</strong><p class="mb-0 mt-2">No Discord, use <strong>/show</strong> e copie o nome exatamente como aparece no bot. A alteração será aplicada ao Elenco Geral e às inscrições ativas.</p></div><div class="row g-3"><div class="col-md-7"><label class="form-label">Nome exato no bot do Discord</label><input class="form-control form-control-lg" name="nome" maxlength="150" required></div><div class="col-md-2"><label class="form-label">OVR</label><input class="form-control form-control-lg" type="number" min="1" max="99" name="overall" required></div><div class="col-md-3"><label class="form-label">Posição</label><select class="form-select form-select-lg" name="posicao">${['GOL','LD','LE','ZAG','VOL','MC','MEI','PD','PE','ATA'].map(item=>`<option>${item}</option>`).join('')}</select></div><div class="col-12"><a class="btn btn-outline-light" data-market-change-value>Alterar valor da compra ou venda</a><small class="d-block text-secondary mt-2">Abre diretamente a movimentação mais recente deste jogador.</small></div></div></div><div class="modal-footer"><button type="button" class="btn btn-outline-light" data-bs-dismiss="modal">Cancelar</button><button class="btn btn-danger">Salvar jogador</button></div></form></div></div>`;document.body.append(modal);
  const form=modal.querySelector('form'),valueLink=modal.querySelector('[data-market-change-value]');cards.forEach(card=>{const button=document.createElement('button');button.type='button';button.className='registration-edit-player';button.title='Edição rápida';button.setAttribute('aria-label',`Editar ${card.dataset.playerName}`);button.innerHTML='<i data-lucide="pencil"></i>';button.onclick=event=>{event.preventDefault();event.stopPropagation();form.querySelector('[name="jogador_geral_id"]').value=card.dataset.playerId;form.querySelector('[name="nome"]').value=card.dataset.playerName;form.querySelector('[name="overall"]').value=card.dataset.overall;form.querySelector('[name="posicao"]').value=card.dataset.position;const query=participant?`participante_id=${encodeURIComponent(participant)}&`:'';valueLink.href=`elenco-geral.php?${query}editar_valor_jogador=${encodeURIComponent(card.dataset.playerId)}`;bootstrap.Modal.getOrCreateInstance(modal).show()};card.append(button)});modal.addEventListener('shown.bs.modal',()=>form.querySelector('[name="nome"]')?.select());if(window.lucide)lucide.createIcons();
});
